/** @type {import("preact").h.JSX.CSSProperties} */
export const SIZE_FULL = {
    width: "100%",
    height: "100%",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_H_CENTER = {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_V_CENTER = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_AUTO = {
    flex: "1 1 auto",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_FIXED = {
    flex: "0 0 auto",
};

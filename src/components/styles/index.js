/** @type {import("preact").h.JSX.CSSProperties} */
export const SIZE_FULL = {
    width: "100%",
    height: "100%",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const WIDTH_FULL = {
    width: "100%",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const HEIGHT_FULL = {
    height: "100%",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_H_CENTER = {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_V_CENTER = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_AUTO = {
    flex: "1 1 auto",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const FLEX_FIXED = {
    flex: "0 0 auto",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const BORDER_RADIUS = {
    borderRadius: "0.5rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const BORDER_NONE = {
    border: "none",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const BORDER_ALL = {
    border: "1px solid var(--var-border-color)",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const BORDER_TOP = {
    borderTop: "1px solid var(--var-border-color)",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const CONTENT_PADDING1 = {
    padding: "0.5rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const CONTENT_PADDING2 = {
    padding: "0.75rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const CONTENT_PADDING3 = {
    padding: "1rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const CONTENT_PADDING4 = {
    padding: "1.5rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const CONTENT_PADDING5 = {
    padding: "2rem",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const TEXT_PRE_WRAP = {
    whiteSpace: "pre-wrap",
    wordBreak: "break-word",
};

/** @type {import("preact").h.JSX.CSSProperties} */
export const SELECTABLE = {
    userSelect: "text",
};
